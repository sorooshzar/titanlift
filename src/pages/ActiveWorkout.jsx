// This page is a shell — the ActiveWorkoutSheet overlay renders the full workout UI.
// It just needs to exist as a route so the sheet knows to show itself in full mode.
export default function ActiveWorkout() {
  return <div className="min-h-screen bg-background" />;
}